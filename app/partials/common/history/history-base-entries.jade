<% if (showMore > 0) { %>
a(href="" title="Show more" class="show-more show-more-comments", translate, translate-values!="{showMore: '<%- showMore %>'}")
    | + Show previous entries ({{showMore}} more)
<% } %>
<% _.each(entries, function(entry) { %>
<%= entry %>
<% }) %>